language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t cwhitaker/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true cwhitaker/docker-react npm run test